# keyboard-repro

Reproduces the bug with the software keyboard as the trigger (`edgeToEdgeEnabled=false`).

See the [repo README](../README.md) for background and the in-app screens for the numbered repro steps.

```bash
yarn install
yarn android
```
