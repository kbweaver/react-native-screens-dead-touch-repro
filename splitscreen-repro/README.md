# splitscreen-repro

Reproduces the bug with split-screen resizing as the trigger, fully edge-to-edge (`edgeToEdgeEnabled=true`). The keyboard is never involved.

See the [repo README](../README.md) for background and the in-app screens for the numbered repro steps.

```bash
yarn install
yarn android
```
